# New update v0.0.7

Changes:
	- `Home` site design.
	- `Layout` Component was replaced with `Document` Component.
	- `user` to `account`.
	- Removed `use-request-all` and all there utility functions.

Added:
	- Some SVG Icons.

Fixed:
	- Bug fixed in `use-request`.
	- Positionin by hovering on `Projects-List` item. 